import { decimalToHex } from "../common.js";
import { colorRayo } from "../../main.js";
import Node from "../node.js";

export const grid_size = 150;

export class World extends Node{
    constructor(ctx) {
        super(ctx);
    }
}